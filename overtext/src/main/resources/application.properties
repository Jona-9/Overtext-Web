spring.application.name=overtext

# Sprint 2 (E2-13) — parametrizado por entorno, con valor por defecto para que
# el proyecto arranque sin configuración en las 6 máquinas del equipo.
server.port=${OVERTEXT_PORT:8080}
spring.thymeleaf.cache=${OVERTEXT_THYMELEAF_CACHE:false}
logging.level.pe.edu.utp.overtext=${OVERTEXT_LOG_LEVEL:INFO}
